class Category {
  constructor(id, name, status, imageUrl) {
    (this.id = id),
      (this.name = name),
      (this.status = status),
      (this.imageUrl = imageUrl);
  }
}

export default Category;
