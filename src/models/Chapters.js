class Chapters {
  constructor(id, bookId, chapterList, create_at, update_at) {
    (this.id = id),
      (this.bookId = bookId),
      (this.chapterList = chapterList),
      (this.create_at = create_at),
      (this.update_at = update_at);
  }
}
export default Chapters;
