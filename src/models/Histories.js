class Histories {
  constructor(uId, chapters, percent, create_at, update_at) {
    (this.uId = uId),
      (this.chapters = chapters),
      (this.percent = percent),
      (this.create_at = create_at),
      (this.update_at = update_at);
  }
}
export default Histories;
