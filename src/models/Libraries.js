class Libraries {
  constructor(bookId, userId, create_at, update_at) {
    (this.bookId = bookId),
      (this.userId = userId),
      (this.create_at = create_at),
      (this.update_at = update_at);
  }
}
export default Libraries;
