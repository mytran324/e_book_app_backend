class Author {
  constructor(id, fullName, status, create_at, update_at) {
    this.id = id;
    this.fullName = fullName;
    this.status = status;
    this.create_at = create_at;
    this.update_at = update_at;
  }
}

export default Author;
