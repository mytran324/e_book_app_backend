class Mission {
  constructor(missionId, coins, detail, name, times, type) {
    (this.missionId = missionId),
      (this.coins = coins),
      (this.detail = detail),
      (this.name = name),
      (this.times = times),
      (this.type = type);
  }
}
export default Mission;
