class User {
  constructor(
    id,
    displayName,
    email,
    photoUrl,
    status
  ) {
    this.id = id;
    this.displayName = displayName;
    this.email = email;
    this.photoUrl = photoUrl;
    this.status = status;
  }
}
export default User;
